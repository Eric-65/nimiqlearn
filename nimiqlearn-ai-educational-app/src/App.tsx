/* Entry wrapper — all implementation lives in AppRoot.jsx (plain JS/JSX). */
// @ts-ignore - AppRoot is intentionally plain JavaScript/JSX in this phase
import AppRoot from "./AppRoot.jsx";

export default AppRoot;
