/* ============================================================
   NimiqLearn — video verification results  (GENERATED FILE)
   ------------------------------------------------------------
   Written by `npm run verify:videos`. Do not edit by hand.

   Each key is a VideoEntry id from videoLibrary.js. An entry
   appears here only after the Wikimedia Commons API answered for
   it, and only becomes playable in the app once a human has
   actually watched it and run:

     npm run verify:videos -- --confirm <id>

   which sets contentConfirmed. The API can prove the file exists
   and report its URL, duration, licence and author; it cannot
   prove the video teaches the right topic in the right language,
   and one candidate in the library is known to be wrong in
   exactly that way.
   ============================================================ */

export const VIDEO_VERIFICATION = {
  generatedAt: null,
  entries: {},
};
