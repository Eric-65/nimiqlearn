{
  "name": "nimiqlearn-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Local development server for NimiqLearn's AI routes. In production these are Vercel serverless functions in ../nimiqlearn-ai-educational-app/api/; this only re-exposes the SAME shared handlers on localhost. Run `npm install` in ../nimiqlearn-ai-educational-app first \u2014 the shared modules resolve `openai` from there.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
